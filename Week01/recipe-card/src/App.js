
// in order to use a component, we must import it
import RecipeCard from './components/RecipeCard';

// declare our app component which returns some JSX

const App = () => {
    return (
        <>
            <RecipeCard />
        </>
    )
}

// when export default, 
export default App;